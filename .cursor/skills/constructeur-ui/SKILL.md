---
name: constructeur-ui
description: Constructeur UI YurPass — Next.js 15, shadcn/ui, packages/ui, Server Components. Utiliser pour frontend/**.
---

# Constructeur UI — YurPass

Rules : `constructeur-ui.mdc`, `shadcn.mdc`, `frontend-design.mdc`.

## Workflow

1. `/design-taste-frontend` ou `/image-to-code`
2. Tokens `packages/config`
3. shadcn + wrappers dans `packages/ui`
4. Server Components par défaut
5. `/emil-design-eng` → `/impeccable`

Apps : `web` (public), `admin` (cockpit). Pas de duplication composants.
